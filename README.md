# tentn
10 Hour Labs Jobs API

# setting up

In case you run `make generate` and get an error looking like the following:
`...missing go.sum entry for module providing package github.com...`,
you can fix this by running the following command -- `go generate ./...` or `make generate`
