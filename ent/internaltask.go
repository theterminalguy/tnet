// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/theterminalguy/tnet/ent/internaltask"
)

// InternalTask is the model entity for the InternalTask schema.
type InternalTask struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// DeletedAt holds the value of the "deleted_at" field.
	DeletedAt *time.Time `json:"deleted_at"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Params holds the value of the "params" field.
	Params string `json:"params,omitempty"`
	// ExecutedBy holds the value of the "executed_by" field.
	ExecutedBy string `json:"executed_by,omitempty"`
	// Succeeded holds the value of the "succeeded" field.
	Succeeded bool `json:"succeeded,omitempty"`
	// Error holds the value of the "error" field.
	Error string `json:"error,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*InternalTask) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case internaltask.FieldSucceeded:
			values[i] = new(sql.NullBool)
		case internaltask.FieldName, internaltask.FieldParams, internaltask.FieldExecutedBy, internaltask.FieldError:
			values[i] = new(sql.NullString)
		case internaltask.FieldCreatedAt, internaltask.FieldUpdatedAt, internaltask.FieldDeletedAt:
			values[i] = new(sql.NullTime)
		case internaltask.FieldID:
			values[i] = new(uuid.UUID)
		default:
			return nil, fmt.Errorf("unexpected column %q for type InternalTask", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the InternalTask fields.
func (it *InternalTask) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case internaltask.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				it.ID = *value
			}
		case internaltask.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				it.CreatedAt = value.Time
			}
		case internaltask.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				it.UpdatedAt = value.Time
			}
		case internaltask.FieldDeletedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field deleted_at", values[i])
			} else if value.Valid {
				it.DeletedAt = new(time.Time)
				*it.DeletedAt = value.Time
			}
		case internaltask.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				it.Name = value.String
			}
		case internaltask.FieldParams:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field params", values[i])
			} else if value.Valid {
				it.Params = value.String
			}
		case internaltask.FieldExecutedBy:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field executed_by", values[i])
			} else if value.Valid {
				it.ExecutedBy = value.String
			}
		case internaltask.FieldSucceeded:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field succeeded", values[i])
			} else if value.Valid {
				it.Succeeded = value.Bool
			}
		case internaltask.FieldError:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field error", values[i])
			} else if value.Valid {
				it.Error = value.String
			}
		}
	}
	return nil
}

// Update returns a builder for updating this InternalTask.
// Note that you need to call InternalTask.Unwrap() before calling this method if this InternalTask
// was returned from a transaction, and the transaction was committed or rolled back.
func (it *InternalTask) Update() *InternalTaskUpdateOne {
	return (&InternalTaskClient{config: it.config}).UpdateOne(it)
}

// Unwrap unwraps the InternalTask entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (it *InternalTask) Unwrap() *InternalTask {
	tx, ok := it.config.driver.(*txDriver)
	if !ok {
		panic("ent: InternalTask is not a transactional entity")
	}
	it.config.driver = tx.drv
	return it
}

// String implements the fmt.Stringer.
func (it *InternalTask) String() string {
	var builder strings.Builder
	builder.WriteString("InternalTask(")
	builder.WriteString(fmt.Sprintf("id=%v", it.ID))
	builder.WriteString(", created_at=")
	builder.WriteString(it.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", updated_at=")
	builder.WriteString(it.UpdatedAt.Format(time.ANSIC))
	if v := it.DeletedAt; v != nil {
		builder.WriteString(", deleted_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteString(", name=")
	builder.WriteString(it.Name)
	builder.WriteString(", params=")
	builder.WriteString(it.Params)
	builder.WriteString(", executed_by=")
	builder.WriteString(it.ExecutedBy)
	builder.WriteString(", succeeded=")
	builder.WriteString(fmt.Sprintf("%v", it.Succeeded))
	builder.WriteString(", error=")
	builder.WriteString(it.Error)
	builder.WriteByte(')')
	return builder.String()
}

// InternalTasks is a parsable slice of InternalTask.
type InternalTasks []*InternalTask

func (it InternalTasks) config(cfg config) {
	for _i := range it {
		it[_i].config = cfg
	}
}
